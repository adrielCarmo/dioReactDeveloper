const nome = 'Adriel'; //  não pode ser alterada diretamente
let aprovado = true; // mais utilizada
var name = "Cleitinho"; // não é mais recomendada, pois é acessível fora do escopo
let nota = 8;

console.log(typeof(nota)); // retorna o tipo da variavel

console.log(`O tipo da variável "name" é ${typeof(nota)}!`)